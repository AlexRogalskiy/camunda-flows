<process-application
        xmlns="http://www.camunda.org/schema/1.0/ProcessApplication"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <process-archive>
        <properties>
            <property name="isDeleteUponUndeploy">false</property>
            <property name="isScanForProcessDefinitions">true</property>

            <!-- below parameter can be used to provide path onfilesystem -->
            <!-- it cause problems for packaged spring boot jar and not load bpmn files in classpath -->
            <!--property name="resourceRootPath">flows</property -->
        </properties>
    </process-archive>

</process-application>